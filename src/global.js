module.exports = {
    config: {
        locale: {
            name: 'id-ID',
            date: { 
                format: 'DD MMMM YYYY' 
            },
            currency: {
                style: 'decimal',
                minimumFractionDigits: 2,
                maximumFractionDigits: 2

            },
            decimal: {
                style: 'decimal',
                minimumFractionDigits: 2,
                maximumFractionDigits: 2
            }
        }
    }
}
